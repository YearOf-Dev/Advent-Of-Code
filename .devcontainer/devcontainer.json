{
  "name": "Advent of Code",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--sysctl", "net.ipv6.conf.all.disable_ipv6=1"],
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go",
        "rust-lang.rust-analyzer",
        "ms-python.python",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
  //"postCreateCommand": "npm install && cd cli && npm install"
}